name: Checkout

on:
  pull_request:
    branches:
      - master
    types:
      - closed

jobs:
  checkout:
    name: Checkout
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6
    - uses: ./.github/actions/checkout


